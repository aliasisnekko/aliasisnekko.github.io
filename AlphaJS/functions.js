//          This is how a basic lvl 1 funtion is created and called.


//          This is how you create parameters for a funtions

function greeting(firstName) { //this declares the function
    firstName = prompt('Please enter your first name');
    return 'Hello, ' + firstName;
}
greeting(firstName);



// function getMilk(bottles) {
//     var cost = bottles * 1.5; //This now stores "bottles" so the robot knows what it cost
// }
//getMilk(5); //"bottles" is stored as an input and can be placed inside the parenthesis for robot to use to calc cost
// function getMilk(bottles) {
//     var cost = bottles * 1.5; //This now stores "bottles" so the robot knows what it cost
// }
//getMilk(5); //"bottles" is stored as an input and can be placed inside the parenthesis for robot to use to calc cost